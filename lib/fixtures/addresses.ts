import { payload as success } from "./addresses/2000-addresses";
import { payload as empty } from "./addresses/2000-addresses-empty";
import { payload as mrSuccess } from "./addresses/2000-addresses-mr";

export const addresses = {
  success,
  empty,
  multipleResidence: {
    success: mrSuccess,
  },
};

