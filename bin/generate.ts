import { writeFileSync } from "fs";
import { join } from "path";

// const resources = require("../dist/index");
// import { PostcodesResource } = resources;

// const httpAgent = {
//   request: options => {
//   }
// };

// const write = ({ content, fileName }) => {
//   const filePath = join("./", fileName);
//   const encoding = "utf8";
//   writeFileSync(filePath, content, { encoding });
// };

// PostcodesResource.generateFixtures({ httpAgent })
//   .then(fixtures => fixtures.forEach(write))
//   .catch(error => {
//     console.log(error);
//   });
